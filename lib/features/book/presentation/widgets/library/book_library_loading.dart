import 'package:flutter/material.dart';

class BookLibraryLoading extends StatelessWidget {
  const BookLibraryLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
