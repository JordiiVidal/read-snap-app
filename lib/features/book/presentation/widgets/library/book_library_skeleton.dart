import 'package:flutter/material.dart';

class BookLibrarySkeleton extends StatelessWidget {
  const BookLibrarySkeleton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
